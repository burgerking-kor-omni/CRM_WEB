/* Korean initialisation for the jQuery UI multiselect plugin. */
/* Written by Charles SANQUER (charles.sanquer@spyrit.net). */

(function ( $ ) {

$.extend($.ech.multiselect.prototype.options, {
	minWidth:150,
	checkAllText: '전체',
	uncheckAllText: '해제',
	noneSelectedText: '선택안됨',
	selectedText: '#개 선택됨'
});

})( jQuery );
